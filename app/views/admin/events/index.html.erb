<div class="underlay-photo" >
  <div style='height: 500px;'>
    <div class="col-sm-12 col-md-12 col-lg-5 px-5 px-sm-0 mx-auto pt-5">
      <h3 class="text-center">イベント一覧</h3>
      <% @events.each do |event| %>
        <div class="col-md-12 mt-3 border d-flex">
            <div class="float-none mt-2">
              <%= link_to admin_event_path(event.id) do %>
              <h5 class="float-left"><%= event.event_name %></h5>
              <% end %>
              <!--lメソッドでDRY書式に指定-->
              <p class="float-left small">　<%= l event.event_date %><%= l event.event_time %></p>

              <div class="float-left">
                <!--< %= link_to user_path(event.user_id) do %>-->
                  <p>　<%= event.user.name %></p>
                <!--< % end %>-->
              </div>

              <div class="float-right ml-3">
                <%= link_to edit_admin_event_path(event.id), class: "btn btn-sm btn-secondary" do %>
                  <i class="fas fa-pen"></i>
                <% end %>
                <%= link_to admin_event_path(event.id), method: :delete, class: "btn btn-sm btn-danger" do %>
                  <i class="fas fa-trash-alt"></i>
                <% end %>
              </div>
          <!--< % end %>-->
              <div class="float-left">
                <p><%= event.event_content %></p>
              </div>
            </div>
        </div>


      <% end %>

    </div>
  </div>
</div>